//删除值是val的结点
    public static Node deleteValue(int val, Node head){
        Node cur = head;
        while (cur.next!= null){
//            Node last = cur.next;
            if(cur.value == val){
                cur.next = cur.next.next;
            }
            cur = cur.next;
        }
        return head;
    }
